import { createTheme } from '@mui/material/styles';

const theme = createTheme({
    palette: {
        type: 'light',
        primary: {
          main: '#4444a8',
        },
        secondary: {
          main: '#ff7ac7',
        },
      },
});

export default theme;